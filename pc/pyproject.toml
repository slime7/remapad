[project]
name = "remapad-bridge"
version = "0.1.0"
description = "Remapad 的 PC 侧工具（remapadctl）：桥接转发、串口命令行、实机截图与固件 OTA"
requires-python = ">=3.10"
dependencies = [
    "customtkinter>=6.0.0",
    "hidapi>=0.14.0",
    "sounddevice>=0.4.6",
]
