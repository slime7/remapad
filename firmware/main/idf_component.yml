# The PocketJS components are discovered from the POCKETJS_ROOT checkout through
# EXTRA_COMPONENT_DIRS in firmware/CMakeLists.txt. Only their own manifests
# pull the Registry dependencies (espressif/quickjs-ng for pocketjs_guest).
dependencies:
  idf: ">=6.0,<6.2"
